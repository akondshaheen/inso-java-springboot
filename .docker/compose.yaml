services:
  # The name of this service is "database"
  database:
    # Official Postgres image from DockerHub (we use the last version)
    container_name: incident-postgres
    image: "postgres:18.2"

    # By default, a Postgres database is running on the 5432 port.
    # If we want to access the database from our computer (outside the container),
    # we must share the port with our computer's port.
    # The syntax is [port we want on our machine]:[port we want to retrieve in the container]
    # Note: We are free to change our computer's port,
    # but take into consideration that it will change the way
    # We are connecting to your database.
    ports:
      - 5432:5432

    environment:
      POSTGRES_USER: admin # The PostgreSQL user (useful to connect to the database)
      POSTGRES_PASSWORD: admin # The PostgreSQL password (useful to connect to the database)
      POSTGRES_DB: incident-db # The PostgreSQL default database (automatically created at first launch)

    ####
    ## If we want to use a `.env` file, please comment the `environment` section.
    ####

    # The `env_file` tag allows us to declare an environment file
    #env_file:
    #  - .env # The name of your environment file (the one at the repository root)

    ####
    ## If we want to persist the database data, please uncomment the lines below.
    ####

    # The `volumes` tag allows us to share a folder with our container.
    # Its syntax is as follows: [folder path on our machine]:[folder path to retrieve in the container]
    volumes:
      # In this example, we share the folder `db-data` in our root repository, with the default PostgreSQL data path.
      # It means that every time the repository is modifying the data inside
      # of `/var/lib/postgresql/data/`, automatically the change will appear in `db-data`.
      # We don't need to create the `db-data` folder. Docker Compose will do it for you.
      - ./postdb-data/:/var/lib/postgresql/